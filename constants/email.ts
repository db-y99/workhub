/**
 * Email validation constants
 */

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

/** URL logo dùng trong tất cả email (absolute URL để hiển thị đúng trong client mail) */
export const EMAIL_LOGO_URL = "https://workhub.y99.info/logo.png";

